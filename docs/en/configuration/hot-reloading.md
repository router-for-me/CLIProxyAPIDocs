# Hot Reloading

The server watches the config file and the `auth-dir` for changes and reloads clients and settings automatically.

You can add or remove `Gemini CLI` / `Codex` / `Claude Code` / `Qwen Code` / `iFlow` token JSON files while the server is running; no restart is required.
